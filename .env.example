# انسخ هذا الملف إلى .env وأدخل القيم الحقيقية
# cp .env.example .env

SECRET_KEY=replace-with-a-long-random-string

DB_HOST=localhost
DB_NAME=zappimiw_nsh
DB_USER=zappimiw_nsh
DB_PASS=your-database-password

# كود مشرف HSE (يُمنح للضباط عند التسجيل)
HSE_SUPERVISOR_CODE=your-supervisor-code

# إشعارات APNs (iOS Push Notifications)
APNS_KEY_ID=
APNS_TEAM_ID=
APNS_KEY_PATH=
APNS_BUNDLE_ID=
